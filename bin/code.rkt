; Generated by SML -> Racket 🚀 compiler (`https://gitlab.com/seckmaster/atheris-swift/tree/sml`)

#lang racket

(define (f g)
  (lambda (x)
    (+ (g (+ x 1)) 1)))
f
(define (g g)
  (lambda (x)
    (+ (g (+ x 1)) 1)))
g
